/**
 * This is required for element rendering to be possible
 * @type {PlatformElement}
 *
 * since everything's done in css, there really isn't anything that needs to happen here.
 */

(function() {
	var PricingTable = PlatformElement.extend({
		initialize: function() { }
	});

	return PricingTable;
})();